<div class="row">

  <div class="col-md-4">
    <%= image_tag book.cover.url(:medium) %>
  </div>

  <div class="col-md-6">
    <h3><%= link_to book.title, book %></h3>
    <p><%= book.date_read.strftime("%B %Y") %></p>
    <p><%= book.rating %> / 10</p> 
    <%= book.abstract %>
  </div>

</div>